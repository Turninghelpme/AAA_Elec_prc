# 2026-04-18 操作日志

## 今日已完成事项

### 1. Jetson 连通性确认

- 确认开发机能够访问 `192.168.19.123`
- 确认 SSH 端口 `22` 已开启
- 已成功通过 SSH 登录 Jetson

### 2. PEAK Linux 驱动安装

- 检查了 PEAK 驱动压缩包内容和 README
- 确认 Jetson 端具备构建条件：
  - 内核头文件可用
  - `gcc`、`g++`、`make` 可用
  - `sudo` 工作正常
- 已将驱动安装包上传到 Jetson
- 已完成 PEAK Linux 驱动的编译与安装
- 已加载 `pcan` 内核模块

### 3. PEAK 驱动测试结果

- 检测到的驱动版本：`8.17.0`
- 检测到的 PCAN-Basic 版本：`4.8.0.5`
- 结论：驱动安装成功，但测试时未检测到 PEAK 硬件设备

### 4. 关闭 Jetson 桌面环境

- 将默认启动目标从 `graphical.target` 改为 `multi-user.target`
- 停止了当前运行中的桌面显示管理器
- 确认关闭桌面后 SSH 服务仍保持可用

### 5. 固定 Jetson IP 地址

- 检查了 Jetson 当前网络配置
- 确认活动网卡为 `wlan0`
- 确认当前 Wi-Fi 配置文件 `iQOO Neo9` 由 NetworkManager 管理
- 已将该配置文件改为静态 IP：
  - IP：`192.168.19.123/24`
  - 网关：`192.168.19.223`
  - DNS：`192.168.19.223`
- 配置重载成功，SSH 会话未中断
- 已确认实时默认路由显示为 `proto static`

### 6. USB 摄像头检查

- 检查了 USB 设备、视频节点、V4L2 设备信息以及摄像头相关 `dmesg`
- 已识别到 USB 摄像头：
  - 名称：`icspring camera`
  - USB ID：`32e6:9211`
  - 设备节点：`/dev/video0`
- 确认 `jetson` 用户具有 `video` 组权限
- 验证了支持的采集格式包含 `MJPG` 和 `YUYV`
- 已通过 OpenCV 完成真实画面采集测试
- 在 `640x480` 分辨率下采集成功
- 测试图片已保存在 Jetson：
  - `/home/jetson/codex-install/usb_camera_test_2026-04-18.jpg`
- 已拉取本地副本到项目目录：
  - `D:\OpenCV\artifacts\usb_camera_test_2026-04-18.jpg`

### 7. USB 摄像头实时串流

- 为 Jetson 编写了轻量级 MJPEG HTTP 串流脚本
- 项目内脚本位置：
  - `D:\OpenCV\tools\jetson_usb_mjpeg_stream.py`
- 已上传到 Jetson：
  - `/home/jetson/codex-install/jetson_usb_mjpeg_stream.py`
- 已使用 Jetson 用户身份将其作为临时 `systemd` 服务启动
- 已确认服务运行正常，并监听 `8090` 端口
- 当前可访问的接口：
  - `http://192.168.19.123:8090/`
  - `http://192.168.19.123:8090/stream.mjpg`
  - `http://192.168.19.123:8090/snapshot.jpg`
- 验证过程中成功远程抓拍，并已保存到本地：
  - `D:\OpenCV\artifacts\usb_camera_live_snapshot.jpg`

### 8. 红色激光点检测演示

- 新建了红色激光点检测演示脚本：
  - `D:\OpenCV\tools\jetson_red_laser_demo.py`
- 已上传到 Jetson：
  - `/home/jetson/codex-install/jetson_red_laser_demo.py`
- 为释放 `/dev/video0`，已停止之前的普通摄像头串流服务
- 已启动新的 Jetson 临时服务：
  - `jetson-red-laser-demo.service`
- 演示服务绑定端口 `8091`
- 已实现以下接口：
  - `/`
  - `/stream.mjpg`
  - `/snapshot.jpg`
  - `/status.json`
- 已在返回画面中叠加以下信息：
  - 红色激光点位置
  - FPS
- 已从 Windows 主机验证主页面、JSON 状态接口和抓拍接口
- 验证抓拍图已保存到本地：
  - `D:\OpenCV\artifacts\red_laser_demo_snapshot.jpg`

## 当前状态

- Jetson 已切换为命令行 + SSH 的开发模式
- 图形界面默认不再启动
- Jetson 当前在 `iQOO Neo9` Wi-Fi 配置文件下使用固定 IP
- PEAK 驱动已经安装完成，待硬件接入后即可继续验证
- 当前连接的 USB 摄像头能够在无桌面环境下被识别并成功采集图像

## 重要说明

本项目已明确允许记录明文凭据。

## 2026-04-18 晚间补充

### 9. 黑胶布内外围取中线代码整理

- 已将当前最新实时检测脚本保存在：
  - `D:\OpenCV\tools\jetson_red_laser_demo.py`
- 当前版本已尝试输出：
  - `outer_corners`
  - `inner_corners`
  - `corners`
- 其中 `corners` 表示黑胶布中线结果

### 10. Jetson 实时服务恢复

- 晚间重新确认 Jetson 已恢复联网
- 已重新连接 `192.168.19.123`
- 确认 `22` 端口已开放
- 当前实时检测服务重新拉起成功
- 当前运行方式不是 `systemd-run`，而是 `jetson` 用户后台常驻进程

### 11. 当前需要重点关注的问题

- 黑胶布“内外围取中线”方案已经接入代码
- 但在画面中只出现黑框一部分、或者大面积外白纸进入画面时，`inner_corners` 仍可能跳到错误白区
- 当前这套方案属于“已经可运行，但仍需继续收紧”的状态

### 12. 下次建议的接手顺序

建议下次回来按以下顺序继续：

1. 先看 `START_HERE.md`
2. 再看 `notes/code-map.md`
3. 然后看 `notes/red-laser-demo.md`
4. 最后再进入 Jetson 页面和代码继续调试

### 13. 本地代码快照已完成

- 已创建本地代码快照目录：
  - `D:\OpenCV\backups\2026-04-18-code-snapshot`
- 已创建压缩包：
  - `D:\OpenCV\backups\2026-04-18-code-snapshot.zip`
- 最新备份入口：
  - `D:\OpenCV\backups\LATEST.txt`
- 当前快照已保存：
  - `jetson_red_laser_demo.py`
  - `jetson_red_laser_demo.cpp`
  - `jetson_usb_mjpeg_stream.py`
  - `peak-linux-driver-8.17.0 (1).tar.gz`
- 已校验 Jetson 端与本地 `tools/` 中的三个主代码文件 SHA256 一致

### 14. 一键同步脚本已就绪

- 已新增：
  - `D:\OpenCV\tools\sync_jetson_code_backup.py`
  - `D:\OpenCV\tools\sync_jetson_code_backup.cmd`
- 用途：
  - 从 Jetson 的 `/home/jetson/codex-install` 拉取当前代码
  - 在 `D:\OpenCV\backups\` 下生成新的带时间戳快照
  - 自动更新 `LATEST.txt`
  - 自动生成 `README.md`、`manifest.sha256.txt`、`.zip`
- 已实际执行成功，生成：
  - `D:\OpenCV\backups\2026-04-18-210221-code-sync`
  - `D:\OpenCV\backups\2026-04-18-210221-code-sync.zip`
